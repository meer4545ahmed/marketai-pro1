Build a complete end-to-end machine-learning financial market prediction platform called “MarketPulse AI”. I want you to handle the entire project yourself, including finding and downloading a suitable publicly available historical financial-market dataset, preprocessing the data, engineering technical-analysis features, training multiple machine-learning models, evaluating them correctly, selecting the best-performing model, saving the trained model and preprocessing pipeline, and then building a polished web application around the trained model. Do not wait for me to upload a dataset unless absolutely necessary; search for and use a reliable publicly available historical dataset containing XAUUSD (Gold/USD) or, if XAUUSD data is not sufficiently available, use another high-quality financial dataset and clearly document the choice. Prefer a dataset with OHLCV-style historical market data and enough historical observations for meaningful machine-learning training. The primary objective is to predict the direction of the next candle/time period as BULLISH or BEARISH rather than attempting to guarantee an exact future price. Start by inspecting the dataset and automatically determine its columns, date/time format, missing values, duplicates, data frequency, and available historical range. Clean the dataset carefully and avoid data leakage. Engineer meaningful financial features including returns, price momentum, candle body size, high-low range, upper and lower wicks, EMA 9, EMA 21, SMA 20, SMA 50, RSI, MACD, MACD signal, Bollinger Bands, ATR, rolling volatility, distance from moving averages, and other useful non-leaking technical features where appropriate. Create the target variable based on whether the next candle closes higher or lower than the current candle, ensuring that future information is never accidentally included in the input features. Because this is time-series financial data, DO NOT randomly shuffle the dataset before splitting it. Use chronological train/validation/test splits, for example approximately 70% training, 15% validation, and 15% final testing, and clearly display the date ranges used for each split. Train and compare several machine-learning algorithms, including Logistic Regression as a baseline, Random Forest, XGBoost, and optionally a neural-network/LSTM model if the dataset and computational resources make it practical. Use appropriate preprocessing such as scaling where required, handle class imbalance if it exists, and use the validation set for model selection. Evaluate every model using Accuracy, Precision, Recall, F1 Score, ROC-AUC, confusion matrix, and other useful classification metrics rather than relying only on accuracy. Select the best model based on robust validation performance and save it using joblib or pickle along with the preprocessing/scaler object, feature names, model configuration, training metadata, and evaluation metrics. Also generate feature-importance or SHAP explainability data for the selected model so the web application can explain why a particular prediction was made. Do not fabricate any performance results; every number shown in the application must come from the actual trained models and evaluation process. After completing the machine-learning pipeline, build a professional full-stack web application around it. Use a Python FastAPI or Flask backend and a modern React frontend, with a clean component-based architecture. The website should look like a premium fintech/trading analytics SaaS product rather than a basic college project, using a dark professional trading-terminal interface, responsive layouts, smooth animations, modern typography, interactive charts, clean cards, subtle gradients, and excellent spacing. The main dashboard should display the selected asset, latest available market price, market trend, model prediction, bullish probability, bearish probability, prediction horizon, model name, and prediction timestamp. Create an interactive candlestick chart using the historical dataset and allow users to overlay EMA 9, EMA 21, SMA 50, Bollinger Bands, and other indicators. Create separate visual charts for RSI, MACD, volatility, and historical price movement. Build a large AI Prediction card that displays something such as “BULLISH — 76.4% probability” or “BEARISH — 68.2% probability”, but make it clear that this is a machine-learning probability and not guaranteed financial advice. Add a Model Performance page displaying the actual Accuracy, Precision, Recall, F1 Score, ROC-AUC, confusion matrix, training period, testing period, dataset size, class distribution, and selected model. Add a Model Comparison section showing the actual results of Logistic Regression, Random Forest, XGBoost, and LSTM if trained. Create an Explainable AI section using SHAP or model feature importance that visually shows which factors influenced the current prediction, for example RSI, MACD, EMA trend, momentum, ATR, volatility, and price range, using only actual calculated values. Add a Historical Predictions page that runs the model over the test dataset and displays predicted versus actual direction, prediction probability, correct/incorrect predictions, cumulative prediction accuracy, bullish/bearish distribution, and other useful analytics. Add a Data Explorer page where users can inspect the downloaded dataset, filter dates, view statistics, and visualize market behavior. Add a model information page explaining the dataset, preprocessing pipeline, features, algorithms tested, model-selection methodology, and limitations. Build a prediction API endpoint that accepts the required market features or a CSV file and returns the prediction, probability, feature values, and explanation. Add CSV upload functionality so users can upload another compatible historical market dataset later, with automatic validation and clear errors when required columns are missing. Keep the project modular so additional assets such as EURUSD, GBPUSD, USDJPY, EURJPY, and BTCUSD can be added later without rewriting the entire application. Also design the backend so a future live-market-data API can replace the static dataset without changing the frontend architecture. Create a News Sentiment module as an extensible feature for the next version: it should be designed to accept financial-news headlines and eventually combine sentiment scores with technical indicators, but do not pretend to have live news data if no free reliable source is available. If a suitable free public news dataset can be obtained, optionally train a simple sentiment-analysis component separately and clearly show whether sentiment is currently enabled. Most importantly, implement the complete machine-learning pipeline in code so the project is reproducible: dataset acquisition, data cleaning, feature engineering, target generation, chronological splitting, model training, evaluation, model selection, serialization, and prediction must all be automated through scripts or notebooks inside the project. Create folders such as data, models, training, backend, frontend, services, features, reports, and configuration as appropriate. Generate a README containing exact instructions for running the project, retraining the models, replacing the dataset, starting the backend, starting the frontend, and understanding the ML pipeline. Include a requirements.txt or equivalent dependency file and make the project easy to run inside Replit. If the first dataset or model performs poorly, do not simply fake better results; inspect the data, improve legitimate feature engineering or preprocessing, compare models again, and document the changes. Avoid look-ahead bias, future-data leakage, random time-series shuffling, and unrealistic claims of guaranteed prediction accuracy. The final product should feel like a genuine AI financial analytics platform that demonstrates Data Science, machine learning, time-series feature engineering, explainable AI, backend development, frontend development, data visualization, and model deployment. At the end, provide a clear project summary showing exactly which dataset was used, where it came from, which features were engineered, which algorithms were trained, which model won, the actual evaluation metrics, where the trained model was saved, how the prediction API works, and how the complete application can be demonstrated to a college professor or potential company. Build the project completely rather than giving me only instructions or a prototype; create the actual working files, code, ML pipeline, trained model, backend, frontend, charts, dashboard, documentation, and sample prediction workflow.

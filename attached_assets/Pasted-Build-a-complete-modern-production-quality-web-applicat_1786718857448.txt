Build a complete, modern, production-quality web application called “MarketPulse AI”, an AI-powered financial market analysis and price-direction prediction platform. This is a Data Science college project that will use a machine-learning model trained separately in Google Colab, so DO NOT train the model inside Replit unless explicitly required later. The trained model will be provided as a file such as .pkl, .joblib, or another compatible serialized model, along with any preprocessing objects such as scalers, encoders, feature lists, and model metadata. Build the application so these trained files can be placed into the project and loaded by the backend securely and reliably. The primary purpose of the application is to analyze historical and current market data for XAUUSD (Gold/USD) initially and predict the likely next-candle price direction as either BULLISH or BEARISH, together with a probability/confidence score. The application must clearly state that predictions are model-generated probabilities and not guaranteed future prices or financial advice. Use a professional fintech-style interface that looks like a real SaaS product rather than a basic college project. Create a responsive dashboard with a dark premium trading-terminal aesthetic, smooth animations, modern typography, clean cards, subtle gradients, glowing highlights, and excellent spacing. The main dashboard should contain an asset selector with XAUUSD as the initial asset and architecture that can later support EURUSD, GBPUSD, USDJPY, EURJPY, and BTCUSD. Include a large interactive candlestick chart showing OHLC price data and allow technical indicators such as EMA 9, EMA 21, SMA 50, RSI, MACD, Bollinger Bands, and ATR to be displayed or hidden. Create a prominent AI Prediction card showing the current prediction, for example “BULLISH”, the probability percentage, prediction horizon, timestamp, and a visual confidence meter. The backend should accept market data containing Open, High, Low, Close, Volume where available, calculate or consume the same engineered features used during model training, and pass the correctly formatted feature vector into the saved model. Make the feature pipeline modular so the exact feature names and ordering can be configured from a metadata file instead of hardcoding assumptions. Include validation that checks whether all required model features are available before making a prediction and display a clear error if something is missing. Build an API backend using Python with FastAPI or Flask, with clean endpoints for health checks, market data, technical indicators, predictions, model information, and feature explanations. Organize the backend into clean modules such as API routes, model loading, preprocessing, feature engineering, prediction service, configuration, and utilities. The frontend can use React with a professional component-based architecture. Include a dashboard page, prediction page, model performance page, market analytics page, and settings/about page. The Model Performance page should display metrics imported from a JSON metadata file generated during Colab training, including Accuracy, Precision, Recall, F1 Score, ROC-AUC, confusion matrix, training/testing period, selected model name, and dataset information. Do not invent performance numbers; if metadata is unavailable, display “Not available” instead. Include a Model Comparison section capable of displaying Logistic Regression, Random Forest, XGBoost, and LSTM results if those metrics are provided by the training pipeline. Include an Explainable AI section using SHAP values or precomputed SHAP/feature-importance data generated from Colab. Display the top factors influencing the prediction using a clean horizontal bar visualization, clearly distinguishing positive and negative contributions. For example, the system may show RSI, MACD, EMA trend, volatility, ATR, and price momentum as important features, but never fabricate these values. Include historical prediction analytics showing previous model predictions versus actual outcomes, prediction accuracy over time, bullish/bearish distribution, and probability calibration where sufficient data exists. Create a data upload interface that allows the user to upload a CSV containing market data for analysis, with automatic validation, column detection, error reporting, and a preview table. Also create the architecture for future live market-data integration, but initially use uploaded CSV/sample data rather than requiring paid APIs. Include a clearly separated “Live Data Integration — Coming Soon” section so the system can later connect to a market-data provider. Similarly, create a News Sentiment module placeholder designed to eventually receive financial news headlines, calculate sentiment, and combine sentiment with technical features; initially allow this module to remain disabled unless sentiment data is provided. The future architecture should support a combined prediction pipeline of technical indicators plus news sentiment. Add loading states, empty states, API error handling, invalid-file handling, model-loading errors, responsive mobile/tablet layouts, and graceful fallback behavior throughout the application. Store configuration variables in environment variables and never hardcode API keys or secrets. Provide a clear README explaining how to install dependencies, place the trained model files into the correct directory, configure metadata, run the backend, run the frontend, upload market data, and test predictions. Create a sample data directory and sample metadata format so the application can run immediately even before the real trained model is uploaded. Most importantly, make the entire application modular so that after I finish training the XAUUSD model in Google Colab, I can simply upload the trained model, scaler/preprocessor, feature configuration, model metrics, and SHAP information into the designated directories and the Replit application automatically loads them without requiring major code changes. The final result should feel like a professional AI financial analytics SaaS product suitable for demonstrating to college professors, recruiters, and potential companies, while remaining transparent that the system is an experimental machine-learning prediction and analytics tool rather than a guaranteed trading or investment system.
